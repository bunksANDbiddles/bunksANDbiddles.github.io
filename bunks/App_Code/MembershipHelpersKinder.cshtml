@helper AccountSummary() 
{
      
          if(WebSecurity.IsAuthenticated) {         
              <text> 

                             
            <a href="@Href("~/Account/ChangePassword")">
            <h1 class="win-type-body">       @WebSecurity.CurrentUserName </h1>
               </a> 

             
              <a href="@Href("~/Account/Logout")">Logout</a>
               @if (Roles.IsUserInRole(WebSecurity.CurrentUserName, "Admin"))
                            {                 
                                <text>                     
                                  <a href="@Href("~/Admin/Default")">Admin</a>                 
                                </text>  
                                                   
                       }   
         </text>
                     } else {

                                  <text>

        <h1 class="win-type-title">     <a href="@Href("~/Account/LoginKinder")"> &#xE2AF;  Login </a> </h1>
            
              <h1 class="win-type-title">    <a href="@Href("~/Account/RegisterKinder")">&#xE77F; Sign Up</a></h1>

   
         </text> 

    }
 }
 